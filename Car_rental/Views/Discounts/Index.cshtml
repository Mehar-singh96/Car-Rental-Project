@model IEnumerable<Car_rental.Models.Discount>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DiscountPercent)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DiscountPercent)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Points }) |
            @Html.ActionLink("Details", "Details", new { id=item.Points }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Points })
        </td>
    </tr>
}

</table>
